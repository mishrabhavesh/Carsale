<%= form_with(model: registrationstate, local: true) do |form| %>
  <% if registrationstate.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(registrationstate.errors.count, "error") %> prohibited this registrationstate from being saved:</h2>

      <ul>
        <% registrationstate.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
