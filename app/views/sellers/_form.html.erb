<div class="container mt-5 py-4 card shadow-lg p-3 mb-5 bg-white rounded">
<%= form_with(model: seller, local: true) do |form| %>
  <% if seller.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(seller.errors.count, "error") %> prohibited this seller from being saved:</h2>

      <ul>
        <% seller.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="row">
    <div class="col-md-6">
      <div class="field ">
        <%= collection_select(:seller, :city_id, City.all, :id, :name, {},  {:class => 'form-control '}) %>
      </div>
      <div class="field">
        <%= collection_select(:seller, :brand_id, Brand.all, :id, :name, {}, {:class => 'form-control '}) %>
      </div>
      <div class="field">
            <%= collection_select(:seller, :model_id, Model.all, :id, :name, {}, {:class => 'form-control '}) %>
      </div>
      <div class="field">
        <%= collection_select(:seller, :registrationyear_id, Registrationyear.all, :id, :name, {}, {:class => 'form-control '}) %>
      </div>
    </div>
    <div class="col-md-6">  
      <div class="field">
        <%= collection_select(:seller, :registrationstate_id, Registrationstate.all, :id, :name, {}, {:class => 'form-control '}) %>
      </div>
      <div class="field">
        <%= collection_select(:seller, :variant_id, Variant.all, :id, :name, {}, {:class => 'form-control '}) %>
      </div>
      <div class="field">
        <%= collection_select(:seller, :kilometerdriven_id, Kilometerdriven.all, :id, :name, {}, {:class => 'form-control '}) %>
      </div>
    </div>
  </div>    
  <div class="actions text-center mt-4  ">
    <%= form.submit class: " shadow-sm p-3 mb-5  rounded"%>
  </div>
      <% end %>
</div>
