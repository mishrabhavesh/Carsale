<div class="text-center">
	<p>
	  <strong>Name:</strong>
	  <%= @variant.name %>
	</p>

	<%= link_to 'Edit', edit_variant_path(@variant), class: "btn btn-warning m-4" %> 
	<%= link_to 'Back', variants_path , class: 'btn btn-dark m-4' %>
</div>